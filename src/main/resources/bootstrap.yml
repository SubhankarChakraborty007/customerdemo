server:
  port: 8085
  
spring:
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true      label: main
  application:
    name: Customer
  profiles:
    active: dev
 
 
management:
  endpoints:
    web:
      exposure:
        include: '*'   
